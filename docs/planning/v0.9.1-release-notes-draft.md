### Bug Fixes

- **Improves the experience on older clients** — v0.7.x–v0.8.x clients get full-quality LOD sessions again instead of the legacy fallback.

### Configuration

- **Adjusted defaults** — lower default bandwidth caps (15 MiB/s per player, 60 MiB/s global; existing config files keep their values), and servers running without the LOD store get a one-line startup tip explaining how to enable it.
