# v0.8.0 release summary — all three lines, verified

Status: prepared and validated, **held for sign-off**. Tag commands: `v0.8.0-tri-release-report.md` §last. Publish order: 26.1 → 1.21.11 → main. VSS publishing disabled on every line (LSS pair only). No protocol bump (wire 18, unchanged since v0.7.0).

## Release matrix

| | `v0.8.0` (main) | `v0.8.0+mc26.1` | `v0.8.0+mc1.21.11` |
|---|---|---|---|
| Branch @ tip | `main` @ `2649966` | `support/mc26.1` @ `82acf82` | `support/mc1.21.11` @ `61f97ca` |
| MC (Fabric) | 26.2 (`>=26.2 <26.3-`) | 26.1–26.1.2 (`>=26.1 <26.2-`) | 1.21.11 exactly |
| MC (Paper/Purpur) | 26.2 (builds 2–84, STABLE 83+) | 26.1.2 (builds ≤74, STABLE 53+) | 1.21.11 (builds ≤132) |
| Folia | — (no 26.2 Folia exists; flag absent) | 26.1.2 builds 6–8 STABLE, experimental | 1.21.11 build 14 STABLE, experimental |
| Java | 25+ | 25+ | 21+ |
| Fabric Loader | ≥0.18.4 | ≥0.18.4 | ≥0.18.4 |
| Fabric API | ≥0.152.1 (oldest real 26.2 build) | ≥0.146.0 (exists for 26.1/26.1.1/26.1.2; all LSS refs resolve against it) | ≥0.141.0 (0.141.0–0.141.5 published) |
| Voxy (optional) | 0.2.17-alpha–0.2.18-beta (all 26.2 builds; reflection verified on 0.2.18-beta) | 0.2.14-alpha+ (surface byte-verified 0.2.14→0.2.18; bare 26.1/26.1.1 max out at 0.2.15-beta) | 0.2.15-beta / 0.2.16-beta (both verified) |
| AntiXray mod (optional) | 1.4.16 (only 26.2 build; all 9 bound members verified; crash shim active) | 1.4.14–1.4.16 (1.4.16+26.1 verified; crash shim active) | 1.4.12–1.4.14 (1.4.14 verified; **no shim needed** — 1.21.x mod is ThreadLocal-based and null-safe; LSS shim is a deliberate pass-through) |
| AntiXray engine-mode adoption | mode 1: engine list; modes 2/3: LSS ore list @ engine height (PR #58 — the union is bulk terrain) | same | same |
| Paper built-in anti-xray | auto-adopted (fields verified on stable 84) | auto-adopted (verified 26.1.2 + 26.2 jars) | auto-adopted (verified on 1.21.11 jar) |
| Sodium/ModMenu (optional, config screen) | 0.9.0+ / 20.x | 0.8.12+ (0.8.10–0.9.1 exist) | 0.8.12–0.8.13 (Voxy 0.2.16 pins ≤0.8.12) / 17.0.0 |

Cross-line safety: each Fabric jar refuses wrong-MC installs (bounded, prerelease-exclusive ranges, pinned by `FabricModJsonContractTest` per line). Paper's `api-version` is only a floor — the 26.1.2 plugin jar loads untested on a 26.2 server (links cleanly, 153/153 refs resolve; documented, not enforced — Paper has no max-api mechanism).

## Protocol compatibility (per released version)

Protocol history: **15** = v0.2.x–v0.3.0 · **16** = v0.4.0–v0.6.2 (+ `v0.5.0+mc1.21.11`, `v0.6.1+mc1.21.8`) · **17** never shipped · **18** = v0.7.0+ (all three v0.8.0 builds).

Interop is per MC version (vanilla blocks cross-MC joins before LSS is consulted). Within an MC version:

| Peer protocol | v0.8.0 server serves it? | v0.8.0 client gets LODs from it? |
|---|---|---|
| 18 (v0.7.0+) | native | native |
| 16 (v0.4.0–v0.6.2 era) | ✅ `enableV16Compat` (default on) — legacy session at the old pace | ✅ `enableV16ServerCompat` (default on) + on-demand generation via `enableV16Generation` (default on) |
| 15 (v0.2.x–v0.3.0) | ✗ silent no-session (vanilla render distance, no error, no log spam client-side) | ✗ same |

Concrete per line: 26.2 — interops with v0.6.0–v0.7.3; 26.1 — with v0.4.0/v0.4.1/v0.5.0/v0.5.1 (v0.3.0 excluded, silent); 1.21.11 — with v0.5.0+mc1.21.11. Both shims are hard `== 16` gates; wire shapes byte-verified against the old tags.

## Compat-review fixes applied (final round, 3 Opus agents, all findings verified)

- Bounded, prerelease-exclusive MC ranges everywhere + per-line contract tests (an unbounded/`<X` range turned future MC prereleases into mixin-apply crashes).
- Honest dependency floors (main's fabric-api/voxy/sodium floors didn't exist for 26.2; 26.1's voxy floor didn't exist for bare 26.1).
- Main's paperweight bumped alpha→stable (26.2.build.84); all Paper-internal bindings (Moonrise IO/gen, anti-xray config) compile+test green against stable.
- Paper anti-xray config read contained on all lines (unreadable future shape → LSS config-key masking, fail-safe, pinned).
- Stale README claims fixed on all lines (pre-v0.7.0 "newer client on older server = no session"; 1.21.11's false crash-shim claim).
- Notes rewritten minimal/user-facing (below); 26.1 legacy set corrected to v0.4.0–v0.5.1.

Validation on final tips: Tier 1 + 58 Tier 2 gametests + `CI=true` release build + `release_check --version 0.8.0` green on all three; CI green; Folia fresh-backfill/warm-rejoin (+ `paper-dirty-falling-block` on 1.21.11) 0 violations; Paper-platform tip soaks re-run this round (26.1: PASS 40 windows; 1.21.11: PASS 40 windows; both 0 violations).

Remaining manual (optional): live v16 smoke with the staged legacy jars (`test-server.sh` pins v0.6.2 / v0.5.1 per line), 5-min `run-fabric-antixray` eyeball on main (mask rewrite has byte-level + gametest coverage, no live re-check).

## Proposed release notes

### v0.8.0 (main, MC 26.2)

> ### Bug Fixes
> - **Fixes black faces at chunk borders and treetops** — LOD data now carries the sky light for the air around terrain, so leaves and cliff sides no longer render black from one side at certain distances. Run `/lss clearcache` once after updating so already-cached LOD data picks up the fix.
> - **Anti-xray now fully covers LOD data** — On servers with anti-xray enabled, LOD chunk data could still be inspected to locate hidden ores. Masked LOD data now carries no trace of hidden blocks.
> - **LOD loading starts reliably on Paper** — The first batch of LOD requests could be lost at join (recovered a second later). Sessions now start cleanly.
>
> ### Compatibility
> - **Three Minecraft versions at once** — v0.8.0 also ships as `v0.8.0+mc26.1` (MC 26.1.x, experimental Folia support) and `v0.8.0+mc1.21.11` (experimental Folia support). Install the build matching your Minecraft version.
> - **No protocol change** — v0.7.x clients and servers keep working with v0.8.0 unchanged, and the compatibility layer for v0.4.x–v0.6.x clients is untouched.

### v0.8.0+mc26.1

> ### Compatibility
> - **The full v0.8.0 feature set for Minecraft 26.1.x** — Updates this line straight from v0.5.1 with everything since. Fabric: MC 26.1–26.1.2, Java 25+, Fabric API 0.146+. Paper/Purpur: 26.1.2. Folia: 26.1.2 (experimental). Also released: `v0.8.0` (MC 26.2) and `v0.8.0+mc1.21.11`.
> - **Works with v0.4.x–v0.5.1 both ways** — Update the client, the server, or both, in any order. A v0.8.0 server keeps serving older clients; a v0.8.0 client still gets LODs (including newly generated terrain) from older servers.
>
> ### New Features
> - **More reliable LOD loading** — Requests the server drops under load retry automatically within a second: no more permanently missing patches, and terrain keeps streaming while you fly.
> - **Server performance is protected** — LOD disk reads and LOD terrain generation run below normal player chunk loading, so players always win.
> - **LOD terrain fills near-to-far** — Generation no longer jumps ahead of closer missing terrain.
> - **Anti-xray now covers LOD data** — The server's anti-xray (Paper's built-in, or the AntiXray mod on Fabric) is detected automatically and the same ores are masked in LOD data, with no trace of the hidden blocks left in it.
>
> ### Bug Fixes
> - **Fixes black faces at chunk borders and treetops** — LOD data now carries the sky light for the air around terrain, so leaves and cliff sides no longer render black from one side at certain distances. Run `/lss clearcache` once after updating so already-cached LOD data picks up the fix.
> - **LOD sessions start reliably on Paper and Folia** — The first batch of LOD requests can no longer be lost at join.
> - **Fixes server crash with the AntiXray mod (Fabric)** — The two now work together.
> - **Quieter console under heavy disk load** — Slow-disk warnings are summarized instead of flooding the log; affected chunks retry automatically.
>
> ### Configuration
> - New server options: `enableV16Compat` (default on — keeps v0.4.x–v0.5.1 clients working), `useBackgroundReadPriority` (default on — set `false` to restore the old foreground disk reads), `missMemoTtlSeconds` (default 30), and `xrayObfuscation` / `xrayHiddenBlocks` / `xrayMaxBlockHeight` (default `"auto"` adopts the server's anti-xray settings). The old `syncOnLoadConcurrencyLimitPerPlayer` key is retired and ignored.

### v0.8.0+mc1.21.11

> ### Compatibility
> - **The full v0.8.0 feature set for Minecraft 1.21.11** — Updates this line straight from v0.5.0+mc1.21.11 with everything since. Fabric: MC 1.21.11, Java 21+, Fabric API 0.141+. Paper/Purpur: 1.21.11. Folia: 1.21.11 (experimental). Also released: `v0.8.0` (MC 26.2) and `v0.8.0+mc26.1`.
> - **Works with v0.5.0+mc1.21.11 both ways** — Update the client, the server, or both, in any order. A v0.8.0 server keeps serving v0.5.0 clients; a v0.8.0 client still gets LODs (including newly generated terrain) from a v0.5.0 server.
>
> ### New Features
> - **More reliable LOD loading** — Requests the server drops under load retry automatically within a second: no more permanently missing patches, and terrain keeps streaming while you fly.
> - **Server performance is protected** — LOD disk reads and LOD terrain generation run below normal player chunk loading, so players always win.
> - **LOD terrain fills near-to-far** — Generation no longer jumps ahead of closer missing terrain.
> - **Anti-xray now covers LOD data** — The server's anti-xray (Paper's built-in, or the AntiXray mod on Fabric) is detected automatically and the same ores are masked in LOD data, with no trace of the hidden blocks left in it.
>
> ### Bug Fixes
> - **Fixes black faces at chunk borders and treetops** — LOD data now carries the sky light for the air around terrain, so leaves and cliff sides no longer render black from one side at certain distances. Run `/lss clearcache` once after updating so already-cached LOD data picks up the fix.
> - **LOD sessions start reliably on Paper and Folia** — The first batch of LOD requests can no longer be lost at join.
> - **Quieter console under heavy disk load** — Slow-disk warnings are summarized instead of flooding the log; affected chunks retry automatically.
>
> ### Configuration
> - New server options: `enableV16Compat` (default on — keeps v0.5.0 clients working), `useBackgroundReadPriority` (default on — set `false` to restore the old foreground disk reads), `missMemoTtlSeconds` (default 30), and `xrayObfuscation` / `xrayHiddenBlocks` / `xrayMaxBlockHeight` (default `"auto"` adopts the server's anti-xray settings). The old `syncOnLoadConcurrencyLimitPerPlayer` key is retired and ignored.
